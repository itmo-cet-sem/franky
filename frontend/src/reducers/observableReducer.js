let initObservableInfo = {
  login: null,
  github: {},
  stackoverflow: {},
  dockerhub: {}
};

export default (state = initObservableInfo, action) => {
  switch (action.type) {
    default: 
      return state;
  }
};